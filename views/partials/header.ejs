<html>
<head>
<title>My Beautiful Family</title>
<link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">	
<script src="https://code.jquery.com/jquery-git2.min.js"></script>		
</head>
<body>
<div id="background">
	
<div class="ui blue inverted menu">
    <div class="ui container">
        <div class="header item"><i class="heart outline icon"></i>My Beautiful Family</div>
        <a href="/" class="item">Home</a>
        <a href="/blogs/new" class="item">New Post</a>
    </div>
	<div class="right menu">
	
                    <% if(!currentUser){ %>
                        <a href="/login" class="item">Login</a>
                        
                    <% } else { %>
                        <a href="#" class="item">Signed In As <%= currentUser.username %></a>
                        <a href="/logout" class="item">Logout</a>
                    <% } %>
	</div>
</div>
	<div class="ui container">
       <% if(error && error.length > 0) { %>
         <div class="ui negative message"><p><%=error %></p></div>
       <% } %>
       
       <% if(success && success.length > 0) { %>
         <div class="ui positive message"><p><%= success %></p></div>
       <% } %>
    </div>